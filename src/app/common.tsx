// export const validateStringEmpty = (value: string, valueName: string): void => {
//   if(typeof value !== 'string' || value.length === 0){
//     throw new Error(`${valueName} must be non-empty string`);
//   }

// }